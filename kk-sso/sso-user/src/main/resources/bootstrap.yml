server:
  port: 8061
spring:
  application:
    name: sso-user
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
        name: sso-user
#  datasource:
#    url: jdbc:mariadb:///jt-sso?serverTimezone=Asia/Shanghai&characterEncoding=utf8
#    username: root
#    password: root